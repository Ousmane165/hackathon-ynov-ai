services:
  webapp:
    build: ./webapp
    container_name: techcorp-webapp
    ports:
      - "8080:8080"
    environment:
      - OLLAMA_URL=http://host.docker.internal:11434
      - OLLAMA_MODEL=phi35-financial
